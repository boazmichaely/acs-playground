# Copy to acs-playground.local.env (gitignored) and fill in real values.
# RHACS / ACS: Platform Configuration → Integrations → API Token
#
# export CENTRAL=your-central-hostname.example.com
# export TOKEN=your-api-token-here
